pipeline {
   agent any

   stages {
      stage('git copy') {
        steps{
                sh 'git checkout master'
                sh 'git merge -s new_feature'
                }
                }

  }
}